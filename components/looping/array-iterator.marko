static function reverseIterator(array, callback) {
  for (var i=array.length-1; i>=0; i--) {
    callback(array[i]);
  }
}

<ul>
  <li for(color in ['a', 'b', 'c'] | iterator=reverseIterator)>
    ${color}
  </li>
</ul>
